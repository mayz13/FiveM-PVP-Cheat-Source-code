#include "program.hpp"
#include <iostream>
#include "auth.hpp"
void main::program(std::string level, std::string expiry)
{
	if (IsValid == false)
	{
	//	std::cout << "Session Expired\n";
		Sleep(5000);
		exit(0);
	}
	//std::cout << "Your Level: " + level + "\n";
	//std::cout << "Your Expiry: " + expiry + "\n";
	//std::cout << "View Program Cpp to modify if levels\n";
	if (level == "0") 
	{
		// default key
		// program code
	}
	if (level == "1")
	{
		// default key
		// level 1 code
	}

	//system("pause");


}